import 'package:flutter/material.dart';

class InfoItemView extends StatelessWidget {
  const InfoItemView({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [],
    );
  }
}